<Window x:Class="MemoryMonitoring.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MemoryMonitoring.App.Controls"
        mc:Ignorable="d"
        Title="Memory Guardian"
        Width="1560"
        Height="980"
        MinWidth="1360"
        MinHeight="860"
        Background="#F6F9FC"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="8,0,8,10" />
            <Setter Property="Padding" Value="18,14" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Foreground" Value="#202938" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter Margin="{TemplateBinding Padding}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#E6EDF3" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="Padding" Value="20" />
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#202938" />
        </Style>

        <Style x:Key="SubtleTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#6B778C" />
            <Setter Property="FontSize" Value="14" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="216" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Background="#FCFEFF" BorderBrush="#E7EEF5" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="72" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="140" />
                </Grid.RowDefinitions>

                <DockPanel Margin="24,0,24,0" VerticalAlignment="Center">
                    <Border Width="32" Height="32" Background="#0E949E" CornerRadius="10">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="15" FontWeight="Bold" Foreground="White" Text="MG" />
                    </Border>
                    <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" FontSize="20" FontWeight="SemiBold" Foreground="#202938" Text="Memory Guardian" />
                </DockPanel>

                <StackPanel Grid.Row="1" Margin="8,12,8,0">
                    <Button x:Name="DashboardNavButton" Click="DashboardNavButton_OnClick" Style="{StaticResource NavButtonStyle}" Background="#0E949E" Foreground="White" FontWeight="SemiBold" Content="仪表盘" />
                    <Button x:Name="ProcessesNavButton" Click="ProcessesNavButton_OnClick" Style="{StaticResource NavButtonStyle}" Content="进程管理" />
                    <Button x:Name="RulesNavButton" Click="RulesNavButton_OnClick" Style="{StaticResource NavButtonStyle}" Content="规则管理" />
                    <Button x:Name="AutomationNavButton" Click="AutomationNavButton_OnClick" Style="{StaticResource NavButtonStyle}" Content="自动化" />
                    <Button x:Name="HistoryNavButton" Click="HistoryNavButton_OnClick" Style="{StaticResource NavButtonStyle}" Content="历史记录" />
                    <Button x:Name="SettingsNavButton" Click="SettingsNavButton_OnClick" Style="{StaticResource NavButtonStyle}" Content="设置" />
                </StackPanel>

                <Border Grid.Row="2" Margin="16" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Foreground="#202938" FontSize="18" FontWeight="SemiBold" Text="服务运行中" />
                        <TextBlock Margin="0,12,0,0" Style="{StaticResource SubtleTextStyle}" Text="v1.3.0.0" />
                        <TextBlock Margin="0,8,0,0" Style="{StaticResource SubtleTextStyle}" Text="实时采样与自动治理已启用" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="20">
            <Grid x:Name="DashboardPage">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <DockPanel Margin="0,4,0,16">
                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="仪表盘" />
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                        <Border Background="White" BorderBrush="#DDE7EF" BorderThickness="1" CornerRadius="12" Padding="16,10" Margin="0,0,12,0">
                            <TextBlock Foreground="#1E8E3E" FontWeight="SemiBold" Text="自动化：运行中" />
                        </Border>
                        <Button Click="RunTrimAction_OnClick" Background="White" BorderBrush="#DDE7EF" BorderThickness="1" Padding="16,10" Cursor="Hand">
                            <TextBlock Foreground="#202938" FontWeight="SemiBold" Text="立即清理" />
                        </Button>
                    </StackPanel>
                </DockPanel>

                <UniformGrid Grid.Row="1" Margin="0,0,0,16" Rows="1" Columns="6">
                    <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SubtleTextStyle}" Text="内存使用率" />
                            <TextBlock Margin="0,8,0,6" FontSize="34" FontWeight="SemiBold" Foreground="#0E949E" Text="{Binding MemoryLoadText}" />
                            <ProgressBar Height="8" Value="{Binding MemoryLoadValue}" Maximum="100" Foreground="#0E949E" Background="#E4ECF2" />
                            <TextBlock Margin="0,8,0,0" Style="{StaticResource SubtleTextStyle}" Text="{Binding MemorySubtitleText}" />
                        </StackPanel>
                    </Border>
                    <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SubtleTextStyle}" Text="可用内存" />
                            <TextBlock Margin="0,8,0,0" FontSize="28" FontWeight="SemiBold" Foreground="#2C9B43" Text="{Binding AvailableMemoryText}" />
                        </StackPanel>
                    </Border>
                    <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SubtleTextStyle}" Text="提交使用量" />
                            <TextBlock Margin="0,8,0,0" FontSize="28" FontWeight="SemiBold" Foreground="#4169E1" Text="{Binding CommitUsageText}" />
                        </StackPanel>
                    </Border>
                    <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SubtleTextStyle}" Text="系统缓存" />
                            <TextBlock Margin="0,8,0,0" FontSize="28" FontWeight="SemiBold" Foreground="#5B6C8D" Text="{Binding CacheUsageText}" />
                        </StackPanel>
                    </Border>
                    <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SubtleTextStyle}" Text="状态" />
                            <TextBlock Margin="0,8,0,0" FontSize="28" FontWeight="SemiBold" Foreground="#2C9B43" Text="{Binding StatusText}" />
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SubtleTextStyle}" Text="自动化状态" />
                            <TextBlock Margin="0,8,0,0" FontSize="28" FontWeight="SemiBold" Foreground="#1E8E3E" Text="{Binding AutomationStatusText}" />
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <controls:MemoryTrendControl Grid.Row="2" Margin="0,0,0,16" ItemsSource="{Binding MemoryHistory}" />

                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.05*" />
                        <ColumnDefinition Width="1.05*" />
                    </Grid.ColumnDefinitions>

                    <Border Margin="0,0,10,0" Style="{StaticResource CardStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="内存占用 Top 进程" />
                            <DataGrid Grid.Row="1" Margin="0,16,0,0" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" IsReadOnly="True" ItemsSource="{Binding TopProcesses}" GridLinesVisibility="Horizontal" BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="进程" Binding="{Binding Process}" Width="*" />
                                    <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="80" />
                                    <DataGridTextColumn Header="工作集" Binding="{Binding WorkingSet}" Width="110" />
                                    <DataGridTextColumn Header="私有内存" Binding="{Binding PrivateBytes}" Width="110" />
                                    <DataGridTextColumn Header="规则" Binding="{Binding Rule}" Width="90" />
                                    <DataGridTextColumn Header="最近活动" Binding="{Binding LastActive}" Width="90" />
                                    <DataGridTextColumn Header="动作" Binding="{Binding Action}" Width="80" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>

                    <Border Grid.Column="1" Margin="10,0,0,0" Style="{StaticResource CardStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="最近操作" />
                            <DataGrid Grid.Row="1" Margin="0,16,0,0" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" IsReadOnly="True" ItemsSource="{Binding RecentActions}" GridLinesVisibility="Horizontal" BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="时间" Binding="{Binding Time}" Width="90" />
                                    <DataGridTextColumn Header="操作类型" Binding="{Binding Type}" Width="140" />
                                    <DataGridTextColumn Header="目标" Binding="{Binding Target}" Width="*" />
                                    <DataGridTextColumn Header="结果" Binding="{Binding Result}" Width="70" />
                                    <DataGridTextColumn Header="回收内存" Binding="{Binding Reclaimed}" Width="90" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>

            <ScrollViewer x:Name="ProcessesPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <DockPanel Margin="0,4,0,16">
                        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="进程管理" />
                        <Button DockPanel.Dock="Right" Click="RefreshProcesses_OnClick" Background="White" BorderBrush="#DDE7EF" BorderThickness="1" Padding="16,10" Cursor="Hand">
                            <TextBlock Foreground="#202938" FontWeight="SemiBold" Text="刷新列表" />
                        </Button>
                    </DockPanel>

                    <Border Margin="0,0,0,16" Style="{StaticResource CardStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="260" />
                                <ColumnDefinition Width="180" />
                                <ColumnDefinition Width="180" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="ProcessSearchBox" Grid.Column="0" Margin="0,0,16,0" Padding="12,10" VerticalContentAlignment="Center" TextChanged="ProcessSearchBox_OnTextChanged" />
                            <ComboBox x:Name="ProcessCategoryFilter" Grid.Column="1" Margin="0,0,16,0" Padding="8" SelectionChanged="ProcessCategoryFilter_OnSelectionChanged" SelectedIndex="0">
                                <ComboBoxItem Content="全部类别" />
                                <ComboBoxItem Content="白名单" />
                                <ComboBoxItem Content="仅清理" />
                                <ComboBoxItem Content="平衡" />
                                <ComboBoxItem Content="自动清理" />
                            </ComboBox>
                            <CheckBox Grid.Column="2" VerticalAlignment="Center" Content="仅显示高内存进程" />
                            <TextBlock Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Right" Style="{StaticResource SubtleTextStyle}" Text="支持搜索、分类筛选和实时刷新" />
                        </Grid>
                    </Border>

                    <Border Margin="0,0,0,16" Style="{StaticResource CardStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="进程列表" />
                            <DataGrid x:Name="ProcessManagementGrid" Grid.Row="1" Margin="0,16,0,0" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" IsReadOnly="True" SelectionChanged="ProcessManagementGrid_OnSelectionChanged" ItemsSource="{Binding ProcessManagementItems}" GridLinesVisibility="Horizontal" BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="进程名" Binding="{Binding ProcessName}" Width="*" />
                                    <DataGridTextColumn Header="PID" Binding="{Binding ProcessId}" Width="80" />
                                    <DataGridTextColumn Header="内存使用" Binding="{Binding MemoryUsage}" Width="120" />
                                    <DataGridTextColumn Header="私有内存" Binding="{Binding PrivateMemory}" Width="120" />
                                    <DataGridTextColumn Header="最近活动" Binding="{Binding LastActive}" Width="100" />
                                    <DataGridTextColumn Header="规则" Binding="{Binding RuleCategory}" Width="90" />
                                    <DataGridTextColumn Header="推荐动作" Binding="{Binding RecommendedAction}" Width="120" />
                                    <DataGridTextColumn Header="手动动作" Binding="{Binding ManualActions}" Width="120" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.2*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="0.9*" />
                        </Grid.ColumnDefinitions>

                        <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#202938" Text="{Binding SelectedProcessName}" />
                                <TextBlock Margin="0,10,0,0" Style="{StaticResource SubtleTextStyle}" Text="{Binding SelectedProcessHint}" />
                                <TextBlock Margin="0,14,0,0" Foreground="#202938" FontWeight="SemiBold" Text="规则类别" />
                                <TextBlock Margin="0,6,0,0" Style="{StaticResource SubtleTextStyle}" Text="{Binding SelectedRuleCategory}" />
                                <TextBlock Margin="0,14,0,0" Foreground="#202938" FontWeight="SemiBold" Text="推荐动作" />
                                <TextBlock Margin="0,6,0,0" Style="{StaticResource SubtleTextStyle}" Text="{Binding SelectedRecommendedAction}" />
                                <TextBlock Margin="0,14,0,0" Foreground="#202938" FontWeight="SemiBold" Text="工作集 / 私有内存" />
                                <TextBlock Margin="0,6,0,0" Style="{StaticResource SubtleTextStyle}" Text="{Binding SelectedProcessMemory}" />
                                <TextBlock Margin="0,4,0,0" Style="{StaticResource SubtleTextStyle}" Text="{Binding SelectedProcessPrivateMemory}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Margin="12,0,12,0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#202938" Text="手动动作" />
                                <Button Margin="0,16,0,0" Padding="12,10" Click="RunTrimAction_OnClick">Trim 工作集</Button>
                                <Button Margin="0,10,0,0" Padding="12,10" Click="AddSelectedProcessToWhitelist_OnClick">加入白名单</Button>
                                <Button Margin="0,10,0,0" Padding="12,10" Click="MarkSelectedProcessTrimOnly_OnClick">标记为仅清理</Button>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Margin="12,0,0,0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#202938" Text="上次清理结果" />
                                <TextBlock Margin="0,14,0,0" Style="{StaticResource SubtleTextStyle}" Text="动作：Trim 工作集" />
                                <TextBlock Margin="0,8,0,0" Style="{StaticResource SubtleTextStyle}" Text="结果：成功" />
                                <TextBlock Margin="0,8,0,0" Style="{StaticResource SubtleTextStyle}" Text="回收内存：128 MB" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <ScrollViewer x:Name="RulesPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <DockPanel Margin="0,4,0,16">
                        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="规则管理" />
                        <Button DockPanel.Dock="Right" Click="SaveRules_OnClick" Background="#0E949E" BorderBrush="#0E949E" Foreground="White" Padding="16,10" Cursor="Hand">
                            <TextBlock Foreground="White" FontWeight="SemiBold" Text="保存规则" />
                        </Button>
                    </DockPanel>

                    <UniformGrid Margin="0,0,0,16" Rows="1" Columns="4">
                        <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}" Background="#F3FBF6">
                            <StackPanel>
                                <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#22863A" Text="白名单" />
                                <TextBlock Margin="0,10,0,0" FontSize="30" FontWeight="SemiBold" Foreground="#22863A" Text="{Binding WhiteListCountText}" />
                                <TextBlock Margin="0,8,0,0" Style="{StaticResource SubtleTextStyle}" Text="不自动挂起，优先保护关键进程" />
                            </StackPanel>
                        </Border>
                        <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}" Background="#F3F7FF">
                            <StackPanel>
                                <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#245BDB" Text="仅清理" />
                                <TextBlock Margin="0,10,0,0" FontSize="30" FontWeight="SemiBold" Foreground="#245BDB" Text="{Binding TrimOnlyCountText}" />
                                <TextBlock Margin="0,8,0,0" Style="{StaticResource SubtleTextStyle}" Text="允许 Trim，不自动挂起" />
                            </StackPanel>
                        </Border>
                        <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}" Background="#FFF9EF">
                            <StackPanel>
                                <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#C97A00" Text="平衡" />
                                <TextBlock Margin="0,10,0,0" FontSize="30" FontWeight="SemiBold" Foreground="#C97A00" Text="{Binding BalancedCountText}" />
                                <TextBlock Margin="0,8,0,0" Style="{StaticResource SubtleTextStyle}" Text="自动清理与降优先级" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource CardStyle}" Background="#FFF4F4">
                            <StackPanel>
                                <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#D14343" Text="可挂起" />
                                <TextBlock Margin="0,10,0,0" FontSize="30" FontWeight="SemiBold" Foreground="#D14343" Text="{Binding SuspendEligibleCountText}" />
                                <TextBlock Margin="0,8,0,0" Style="{StaticResource SubtleTextStyle}" Text="高压下允许挂起" />
                            </StackPanel>
                        </Border>
                    </UniformGrid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.5*" />
                            <ColumnDefinition Width="0.9*" />
                        </Grid.ColumnDefinitions>

                        <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="规则列表" />
                                <DataGrid x:Name="RulesGrid" Grid.Row="1" Margin="0,16,0,0" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" IsReadOnly="True" SelectionChanged="RulesGrid_OnSelectionChanged" ItemsSource="{Binding RuleItems}" GridLinesVisibility="Horizontal" BorderThickness="0">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="进程名称" Binding="{Binding ProcessName}" Width="*" />
                                        <DataGridTextColumn Header="匹配类型" Binding="{Binding MatchType}" Width="90" />
                                        <DataGridTextColumn Header="类别" Binding="{Binding Category}" Width="90" />
                                        <DataGridTextColumn Header="自动清理" Binding="{Binding AutoTrim}" Width="90" />
                                        <DataGridTextColumn Header="优先级" Binding="{Binding PriorityMode}" Width="100" />
                                        <DataGridTextColumn Header="电源节流" Binding="{Binding PowerThrottling}" Width="90" />
                                        <DataGridTextColumn Header="自动挂起" Binding="{Binding AutoSuspend}" Width="90" />
                                        <DataGridTextColumn Header="冷却时间" Binding="{Binding Cooldown}" Width="90" />
                                        <DataGridTextColumn Header="备注" Binding="{Binding Notes}" Width="120" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>

                        <Border Grid.Column="1" Margin="12,0,0,0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="编辑规则" />
                                <TextBlock Margin="0,16,0,8" Style="{StaticResource SubtleTextStyle}" Text="进程名" />
                                <TextBox x:Name="RuleProcessNameTextBox" Padding="12,10" Text="{Binding SelectedRuleProcessName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock Margin="0,16,0,8" Style="{StaticResource SubtleTextStyle}" Text="类别" />
                                <ComboBox x:Name="RuleCategoryComboBox" Padding="8" SelectionChanged="RuleCategoryComboBox_OnSelectionChanged">
                                    <ComboBoxItem Content="白名单" />
                                    <ComboBoxItem Content="仅清理" />
                                    <ComboBoxItem Content="平衡" />
                                    <ComboBoxItem Content="可挂起" />
                                </ComboBox>
                                <TextBlock Margin="0,16,0,8" Style="{StaticResource SubtleTextStyle}" Text="冷却时间" />
                                <TextBox x:Name="RuleCooldownTextBox" Padding="12,10" Text="{Binding SelectedRuleCooldown, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBlock Margin="0,16,0,8" Style="{StaticResource SubtleTextStyle}" Text="备注" />
                                <TextBox x:Name="RuleNotesTextBox" Height="100" Padding="12" TextWrapping="Wrap" AcceptsReturn="True" Text="{Binding SelectedRuleNotes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Margin="0,16,0,0" Padding="12,10" Click="ApplyRuleEditor_OnClick">应用到列表</Button>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <ScrollViewer x:Name="AutomationPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <DockPanel Margin="0,4,0,16">
                        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="自动化策略" />
                        <Button DockPanel.Dock="Right" Click="SaveAutomationSettings_OnClick" Background="#0E949E" BorderBrush="#0E949E" Foreground="White" Padding="16,10" Cursor="Hand">
                            <TextBlock Foreground="White" FontWeight="SemiBold" Text="保存策略" />
                        </Button>
                    </DockPanel>

                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.95*" />
                            <ColumnDefinition Width="1.2*" />
                        </Grid.ColumnDefinitions>

                        <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock FontSize="20" FontWeight="SemiBold" Foreground="#202938" Text="自动清理总开关" />
                                <TextBlock Margin="0,16,0,0" FontSize="26" FontWeight="SemiBold" Foreground="#0E949E" Text="已启用" />
                                <TextBlock Margin="0,8,0,0" Style="{StaticResource SubtleTextStyle}" Text="当前模式：Balanced（平衡）" />
                                <CheckBox Margin="0,18,0,0" Content="随 Windows 启动" IsChecked="{Binding StartWithWindows, Mode=TwoWay}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Margin="12,0,0,0" Style="{StaticResource CardStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="1*" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.ColumnSpan="2" FontSize="20" FontWeight="SemiBold" Foreground="#202938" Text="触发条件设置" />

                                <TextBlock Grid.Row="1" Margin="0,18,0,8" Style="{StaticResource SubtleTextStyle}" Text="内存使用率阈值" />
                                <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,18,0,8" Style="{StaticResource SubtleTextStyle}" Text="可用内存阈值" />
                                <TextBox x:Name="MemoryThresholdTextBox" Grid.Row="2" Margin="0,0,16,0" Padding="12,10" Text="{Binding MemoryThresholdText, Mode=TwoWay}" />
                                <TextBox x:Name="AvailableThresholdTextBox" Grid.Row="2" Grid.Column="1" Padding="12,10" Text="{Binding AvailableThresholdText, Mode=TwoWay}" />

                                <TextBlock Grid.Row="3" Margin="0,18,0,8" Style="{StaticResource SubtleTextStyle}" Text="持续高压时长" />
                                <TextBlock Grid.Row="3" Grid.Column="1" Margin="0,18,0,8" Style="{StaticResource SubtleTextStyle}" Text="启动延迟清理" />
                                <TextBox x:Name="SustainedPressureTextBox" Grid.Row="4" Margin="0,0,16,0" Padding="12,10" Text="{Binding SustainedPressureText, Mode=TwoWay}" />
                                <TextBox x:Name="StartupDelayTextBox" Grid.Row="4" Grid.Column="1" Padding="12,10" Text="{Binding StartupDelayText, Mode=TwoWay}" />
                            </Grid>
                        </Border>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.2*" />
                            <ColumnDefinition Width="0.9*" />
                        </Grid.ColumnDefinitions>

                        <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock FontSize="20" FontWeight="SemiBold" Foreground="#202938" Text="分级清理策略" />
                                <Border Margin="0,16,0,10" Background="#F7FCFD" BorderBrush="#DCEFF1" BorderThickness="1" CornerRadius="12" Padding="16">
                                    <StackPanel>
                                        <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#202938" Text="等级 1（轻度）" />
                                        <TextBlock Margin="0,6,0,0" Style="{StaticResource SubtleTextStyle}" Text="修剪不活跃的后台进程，释放工作集。" />
                                    </StackPanel>
                                </Border>
                                <Border Margin="0,0,0,10" Background="#FFF9EF" BorderBrush="#F5E2B8" BorderThickness="1" CornerRadius="12" Padding="16">
                                    <StackPanel>
                                        <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#202938" Text="等级 2（中度）" />
                                        <TextBlock Margin="0,6,0,0" Style="{StaticResource SubtleTextStyle}" Text="降低后台进程优先级，并启用节流。" />
                                    </StackPanel>
                                </Border>
                                <Border Margin="0,0,0,10" Background="#FFF4F4" BorderBrush="#F5C8C8" BorderThickness="1" CornerRadius="12" Padding="16">
                                    <StackPanel>
                                        <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#202938" Text="等级 3（强化）" />
                                        <TextBlock Margin="0,6,0,0" Style="{StaticResource SubtleTextStyle}" Text="清理低优先级 Standby 列表并合并内存页。" />
                                    </StackPanel>
                                </Border>
                                <Border Background="#FFF4F4" BorderBrush="#F3C0C0" BorderThickness="1" CornerRadius="12" Padding="16">
                                    <StackPanel>
                                        <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#202938" Text="等级 4（激进）" />
                                        <TextBlock Margin="0,6,0,0" Style="{StaticResource SubtleTextStyle}" Text="仅对白名单外且允许挂起的进程执行挂起。" />
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <StackPanel Grid.Column="1" Margin="12,0,0,0">
                            <Border Margin="0,0,0,16" Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock FontSize="20" FontWeight="SemiBold" Foreground="#202938" Text="保护行为" />
                                    <CheckBox Margin="0,16,0,0" Content="保护前台进程（不清理当前正在使用的应用）" IsChecked="True" />
                                    <CheckBox Margin="0,10,0,0" Content="白名单保护（不允许自动挂起）" IsChecked="True" />
                                    <CheckBox Margin="0,10,0,0" Content="保护网络敏感进程" IsChecked="True" />
                                    <CheckBox Margin="0,10,0,0" Content="新启动进程保护期" IsChecked="True" />
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock FontSize="20" FontWeight="SemiBold" Foreground="#202938" Text="补充设置" />
                                    <TextBlock Margin="0,14,0,8" Style="{StaticResource SubtleTextStyle}" Text="唤醒后延迟" />
                                    <TextBox x:Name="ResumeDelayTextBox" Padding="12,10" Text="{Binding ResumeDelayText, Mode=TwoWay}" />
                                    <TextBlock Margin="0,14,0,8" Style="{StaticResource SubtleTextStyle}" Text="全局冷却时间" />
                                    <TextBox x:Name="CooldownTextBox" Padding="12,10" Text="{Binding CooldownText, Mode=TwoWay}" />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <ScrollViewer x:Name="HistoryPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Margin="0,4,0,16" Style="{StaticResource SectionTitleStyle}" Text="历史记录" />
                    <UniformGrid Margin="0,0,0,16" Rows="1" Columns="3">
                        <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource SubtleTextStyle}" Text="累计记录数" />
                                <TextBlock Margin="0,10,0,0" FontSize="30" FontWeight="SemiBold" Foreground="#0E949E" Text="{Binding HistoryTotalActionsText}" />
                            </StackPanel>
                        </Border>
                        <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource SubtleTextStyle}" Text="成功次数" />
                                <TextBlock Margin="0,10,0,0" FontSize="30" FontWeight="SemiBold" Foreground="#1E8E3E" Text="{Binding HistorySuccessCountText}" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource SubtleTextStyle}" Text="最近动作" />
                                <TextBlock Margin="0,10,0,0" FontSize="24" FontWeight="SemiBold" Foreground="#202938" Text="{Binding HistoryLastActionText}" />
                            </StackPanel>
                        </Border>
                    </UniformGrid>

                    <Border Style="{StaticResource CardStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="动作日志" />
                            <DataGrid Grid.Row="1" Margin="0,16,0,0" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" IsReadOnly="True" ItemsSource="{Binding RecentActions}" GridLinesVisibility="Horizontal" BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="时间" Binding="{Binding Time}" Width="100" />
                                    <DataGridTextColumn Header="动作" Binding="{Binding Type}" Width="160" />
                                    <DataGridTextColumn Header="目标" Binding="{Binding Target}" Width="*" />
                                    <DataGridTextColumn Header="结果" Binding="{Binding Result}" Width="80" />
                                    <DataGridTextColumn Header="回收内存" Binding="{Binding Reclaimed}" Width="110" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <ScrollViewer x:Name="SettingsPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <DockPanel Margin="0,4,0,16">
                        <TextBlock Style="{StaticResource SectionTitleStyle}" Text="设置" />
                        <Button DockPanel.Dock="Right" Click="SaveAutomationSettings_OnClick" Background="#0E949E" BorderBrush="#0E949E" Foreground="White" Padding="16,10" Cursor="Hand">
                            <TextBlock Foreground="White" FontWeight="SemiBold" Text="保存设置" />
                        </Button>
                    </DockPanel>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>

                        <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock FontSize="20" FontWeight="SemiBold" Foreground="#202938" Text="监控" />
                                <TextBlock Margin="0,16,0,8" Style="{StaticResource SubtleTextStyle}" Text="内存阈值" />
                                <TextBox Padding="12,10" Text="{Binding MemoryThresholdText, Mode=TwoWay}" />
                                <TextBlock Margin="0,16,0,8" Style="{StaticResource SubtleTextStyle}" Text="可用内存阈值" />
                                <TextBox Padding="12,10" Text="{Binding AvailableThresholdText, Mode=TwoWay}" />
                                <TextBlock Margin="0,16,0,8" Style="{StaticResource SubtleTextStyle}" Text="持续高压时长" />
                                <TextBox Padding="12,10" Text="{Binding SustainedPressureText, Mode=TwoWay}" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Margin="12,0,0,0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock FontSize="20" FontWeight="SemiBold" Foreground="#202938" Text="延迟与冷却" />
                                <TextBlock Margin="0,16,0,8" Style="{StaticResource SubtleTextStyle}" Text="启动延迟" />
                                <TextBox Padding="12,10" Text="{Binding StartupDelayText, Mode=TwoWay}" />
                                <TextBlock Margin="0,16,0,8" Style="{StaticResource SubtleTextStyle}" Text="唤醒后延迟" />
                                <TextBox Padding="12,10" Text="{Binding ResumeDelayText, Mode=TwoWay}" />
                                <TextBlock Margin="0,16,0,8" Style="{StaticResource SubtleTextStyle}" Text="全局冷却时间" />
                                <TextBox Padding="12,10" Text="{Binding CooldownText, Mode=TwoWay}" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
