<Window x:Class="MemoryMonitoring.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MemoryMonitoring.App.Controls"
        mc:Ignorable="d"
        Title="Memory Guardian"
        Width="1560"
        Height="980"
        MinWidth="1360"
        MinHeight="860"
        Background="#F6F9FC"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="8,0,8,10" />
            <Setter Property="Padding" Value="18,14" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Foreground" Value="#202938" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter Margin="{TemplateBinding Padding}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#E6EDF3" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="Padding" Value="20" />
        </Style>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#202938" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="216" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Background="#FCFEFF" BorderBrush="#E7EEF5" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="72" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="140" />
                </Grid.RowDefinitions>

                <DockPanel Margin="24,0,24,0" VerticalAlignment="Center">
                    <Border Width="32" Height="32" Background="#0E949E" CornerRadius="10">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Foreground="White" Text="🛡" />
                    </Border>
                    <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" FontSize="20" FontWeight="SemiBold" Foreground="#202938" Text="Memory Guardian" />
                </DockPanel>

                <StackPanel Grid.Row="1" Margin="8,12,8,0">
                    <Button x:Name="DashboardNavButton" Click="DashboardNavButton_OnClick" Style="{StaticResource NavButtonStyle}" Background="#0E949E" Foreground="White" FontWeight="SemiBold" Content="仪表盘" />
                    <Button x:Name="ProcessesNavButton" Click="ProcessesNavButton_OnClick" Style="{StaticResource NavButtonStyle}" Content="进程管理" />
                    <Button x:Name="RulesNavButton" Click="RulesNavButton_OnClick" Style="{StaticResource NavButtonStyle}" Content="规则管理" />
                    <Button x:Name="AutomationNavButton" Click="AutomationNavButton_OnClick" Style="{StaticResource NavButtonStyle}" Content="自动化" />
                    <Button x:Name="HistoryNavButton" Click="HistoryNavButton_OnClick" Style="{StaticResource NavButtonStyle}" Content="历史记录" />
                    <Button x:Name="SettingsNavButton" Click="SettingsNavButton_OnClick" Style="{StaticResource NavButtonStyle}" Content="设置" />
                </StackPanel>

                <Border Grid.Row="2" Margin="16" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Foreground="#202938" FontSize="18" FontWeight="SemiBold" Text="服务运行中" />
                        <TextBlock Margin="0,12,0,0" Foreground="#6B778C" FontSize="14" Text="v1.3.0.0" />
                        <TextBlock Margin="0,8,0,0" Foreground="#6B778C" FontSize="14" Text="实时采样与自动治理已启用" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Margin="20">
            <Grid x:Name="DashboardPage">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <DockPanel Margin="0,4,0,16">
                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="仪表盘" />
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                        <Border Background="White" BorderBrush="#DDE7EF" BorderThickness="1" CornerRadius="12" Padding="16,10" Margin="0,0,12,0">
                            <TextBlock Foreground="#1E8E3E" FontWeight="SemiBold" Text="自动化：运行中" />
                        </Border>
                        <Border Background="White" BorderBrush="#DDE7EF" BorderThickness="1" CornerRadius="12" Padding="16,10">
                            <TextBlock Foreground="#202938" FontWeight="SemiBold" Text="立即清理" />
                        </Border>
                    </StackPanel>
                </DockPanel>

                <UniformGrid Grid.Row="1" Margin="0,0,0,16" Rows="1" Columns="6">
                    <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Foreground="#6B778C" FontSize="14" Text="内存使用率" />
                            <TextBlock Margin="0,8,0,6" FontSize="34" FontWeight="SemiBold" Foreground="#0E949E" Text="{Binding MemoryLoadText}" />
                            <ProgressBar Height="8" Value="{Binding MemoryLoadValue}" Maximum="100" Foreground="#0E949E" Background="#E4ECF2" />
                            <TextBlock Margin="0,8,0,0" Foreground="#6B778C" FontSize="13" Text="{Binding MemorySubtitleText}" />
                        </StackPanel>
                    </Border>
                    <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Foreground="#6B778C" FontSize="14" Text="可用内存" />
                            <TextBlock Margin="0,8,0,0" FontSize="28" FontWeight="SemiBold" Foreground="#2C9B43" Text="{Binding AvailableMemoryText}" />
                        </StackPanel>
                    </Border>
                    <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Foreground="#6B778C" FontSize="14" Text="提交使用量" />
                            <TextBlock Margin="0,8,0,0" FontSize="28" FontWeight="SemiBold" Foreground="#4169E1" Text="{Binding CommitUsageText}" />
                        </StackPanel>
                    </Border>
                    <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Foreground="#6B778C" FontSize="14" Text="系统缓存" />
                            <TextBlock Margin="0,8,0,0" FontSize="28" FontWeight="SemiBold" Foreground="#5B6C8D" Text="{Binding CacheUsageText}" />
                        </StackPanel>
                    </Border>
                    <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Foreground="#6B778C" FontSize="14" Text="状态" />
                            <TextBlock Margin="0,8,0,0" FontSize="28" FontWeight="SemiBold" Foreground="#2C9B43" Text="{Binding StatusText}" />
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Foreground="#6B778C" FontSize="14" Text="自动化状态" />
                            <TextBlock Margin="0,8,0,0" FontSize="28" FontWeight="SemiBold" Foreground="#1E8E3E" Text="{Binding AutomationStatusText}" />
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <controls:MemoryTrendControl Grid.Row="2" Margin="0,0,0,16" />

                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.05*" />
                        <ColumnDefinition Width="1.05*" />
                    </Grid.ColumnDefinitions>

                    <Border Margin="0,0,10,0" Style="{StaticResource CardStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="内存占用 Top 进程" />
                            </DockPanel>
                            <DataGrid Grid.Row="1"
                                      Margin="0,16,0,0"
                                      AutoGenerateColumns="False"
                                      HeadersVisibility="Column"
                                      CanUserAddRows="False"
                                      IsReadOnly="True"
                                      ItemsSource="{Binding TopProcesses}"
                                      GridLinesVisibility="Horizontal"
                                      BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="进程" Binding="{Binding Process}" Width="*" />
                                    <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="80" />
                                    <DataGridTextColumn Header="工作集" Binding="{Binding WorkingSet}" Width="110" />
                                    <DataGridTextColumn Header="私有内存" Binding="{Binding PrivateBytes}" Width="110" />
                                    <DataGridTextColumn Header="规则" Binding="{Binding Rule}" Width="90" />
                                    <DataGridTextColumn Header="最近活动" Binding="{Binding LastActive}" Width="90" />
                                    <DataGridTextColumn Header="动作" Binding="{Binding Action}" Width="80" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>

                    <Border Grid.Column="1" Margin="10,0,0,0" Style="{StaticResource CardStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <DockPanel>
                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="最近操作" />
                            </DockPanel>
                            <DataGrid Grid.Row="1"
                                      Margin="0,16,0,0"
                                      AutoGenerateColumns="False"
                                      HeadersVisibility="Column"
                                      CanUserAddRows="False"
                                      IsReadOnly="True"
                                      ItemsSource="{Binding RecentActions}"
                                      GridLinesVisibility="Horizontal"
                                      BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="时间" Binding="{Binding Time}" Width="90" />
                                    <DataGridTextColumn Header="操作类型" Binding="{Binding Type}" Width="110" />
                                    <DataGridTextColumn Header="目标" Binding="{Binding Target}" Width="*" />
                                    <DataGridTextColumn Header="结果" Binding="{Binding Result}" Width="70" />
                                    <DataGridTextColumn Header="回收内存" Binding="{Binding Reclaimed}" Width="90" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>

            <ScrollViewer x:Name="ProcessesPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Margin="0,4,0,16" Style="{StaticResource SectionTitleStyle}" Text="进程管理" />
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#202938" Text="进程筛选与动作面板" />
                            <TextBlock Margin="0,12,0,0" Foreground="#6B778C" Text="该页将承接真实进程筛选、排序、规则切换和手动回收动作，风格已统一到仪表盘样式。" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <ScrollViewer x:Name="RulesPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Margin="0,4,0,16" Style="{StaticResource SectionTitleStyle}" Text="规则管理" />
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#202938" Text="规则分类概览" />
                            <TextBlock Margin="0,12,0,0" Foreground="#6B778C" Text="白名单、仅清理、平衡、可挂起四类规则都会沿用前 3 张图的卡片、边框和高密度表格风格。" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <ScrollViewer x:Name="AutomationPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Margin="0,4,0,16" Style="{StaticResource SectionTitleStyle}" Text="自动化策略" />
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#202938" Text="分级清理策略" />
                            <TextBlock Margin="0,12,0,0" Foreground="#6B778C" Text="该页会继续承接阈值设置、冷却时间、保护行为和分级清理配置。" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <ScrollViewer x:Name="HistoryPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Margin="0,4,0,16" Style="{StaticResource SectionTitleStyle}" Text="历史记录" />
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#202938" Text="历史与效果分析" />
                            <TextBlock Margin="0,12,0,0" Foreground="#6B778C" Text="该页风格已向仪表盘与进程页统一，后续会补时间筛选、趋势图叠加和动作明细。" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <ScrollViewer x:Name="SettingsPage" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Margin="0,4,0,16" Style="{StaticResource SectionTitleStyle}" Text="设置" />
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#202938" Text="通用与高级设置" />
                            <TextBlock Margin="0,12,0,0" Foreground="#6B778C" Text="该页后续会承接采样周期、日志、托盘行为、执行器权限和通知设置，并统一沿用当前页面语言。" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
